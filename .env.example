DB_NAME=sentineldesk
DB_USER=your_postgres_user
DB_PASSWORD=your_postgres_password
DB_HOST=localhost

PORT=5001
# Never set this to 1 on a machine reachable from the network.
# The Werkzeug debugger allows arbitrary code execution.
FLASK_DEBUG=0
